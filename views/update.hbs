{{!-- https://codepen.io/mamislimen/pen/jOwwLvy :- Template Link --}}
<!DOCTYPE html>
<html>

<head>
    <title>Admin Add Student</title>
    <link rel="shortcut icon" href="https://img.icons8.com/avantgarde/512/planner.png" type="image/x-icon"
        sizes="120x120">
    <link rel="stylesheet" type="text/css" href="reg_form.css" />
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@500&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
    <div class="main">
        {{!-- if data FOUND --}}
        {{#if mesg1}}

        <div class="main">
            <input type="checkbox" id="chk" aria-hidden="true" />

            <div class="signup">
                {{#data}}
                <form action="/adminupdatestudent">
                    <label for="chk" aria-hidden="true">Update Student</label>
                    <input type="text" name="full_name" placeholder="Full name" required="" autocomplete="none"
                        value="{{name}}" style="text-transform: capitalize;" />
                    <input type="email" name="email" placeholder="Email" required="" value="{{email}}" />
                    <input type="number" name="phone_number" placeholder="Phone Number" required="" autocomplete="none"
                        value="{{phone_no}}" readonly />

                    <p>
                        <select name="event_name" id="Event Name">
                            <option>{{event_name}}</option>
                            <option value="Valowarz" disabled="true">Valowarz</option>
                            <option value="Design-O-Code" disabled="true">Design-O-Code</option>
                            <option value="Craken" disabled="true">Craken</option>
                        </select>
                    </p>

                    <input type="name" name="clg_name" placeholder="College Name" required="" autocomplete="none"
                        value="{{college_name}}" style="text-transform: capitalize;" />

                    <button>UPDATE</button>

                </form>
                {{/data}}

            </div>
        </div>
        <div class="back">
            <h3><a href="./">&#60;-- Go Back</a></h3>
        </div>

        {{/if}}





        <input type="checkbox" id="chk" aria-hidden="true" />

        <div class="signup">
            <form action="/adminupdatesearch">
                <label for="chk" aria-hidden="true">Search Student</label>
                <input type="text" name="full_name" placeholder="Full name" required="" autocomplete="none" />

                <input type="number" name="phone_number" placeholder="Phone Number" required="" autocomplete="none" />

                <p>
                    <select name="event_name" id="Event Name">
                        <option value="">Select Event Name</option>
                        <option value="Valowarz">Valowarz</option>
                        <option value="Design-O-Code">Design-O-Code</option>
                        <option value="Craken">Craken</option>
                    </select>
                </p>
                <button>Search</button>

                {{!-- Sending message after updating the --}}
                {{#if umesg}}
                <H3>Previous Data Updated Successfully...!</H3>

                {{/if}}




                {{!-- Sending message if data not found --}}
                {{#if mesg2}}
                <h3 style="color: red;">DATA NOT FOUND..!!!</h3>
                {{/if}}
            </form>

        </div>

    </div>
    <div class="back">
        <h3><a href="/adminindex">&#60;-- Go Back</a></h3>
    </div>
</body>

</html>